# Runtime dependencies used by the standalone Python tools and ONNX detector.
# ROS-facing Python packages (rclpy, cv_bridge, message types) are installed by
# rosdep/apt and deliberately do not appear here.
# Keep NumPy 1.x ABI compatibility with ROS Jazzy's apt-provided cv_bridge.
numpy>=1.26,<2
onnxruntime>=1.20,<2
onnx>=1.16,<1.20
opencv-python-headless>=4.10,<5
PyYAML>=6.0,<7
ultralytics>=8.3,<8.4

# Benchmarking, plotting, and tests.
matplotlib>=3.8,<4
pandas>=2.2,<4
# ROS Jazzy's launch_testing plugin targets the Ubuntu 24.04 pytest stack.
pytest>=7.4,<8
pytest-cov>=4.1,<5
