# Generated benchmark artifacts

This directory intentionally contains no checked-in CSV or PNG result. Run the
live benchmark after Gate 6.1; `run_benchmark.py` writes `raw_results.csv` and
`summary.csv`, then `plot_results.py` creates the seven required plots from
those recorded rows only.
