# Demo bags

Generated rosbag directories are stored here and excluded from version
control. Start the projection demo, then run:

```bash
./scripts/record_personal_bag.sh
```

Inspect a recording with `ros2 bag info bags/<bag-name>` and replay it with
`ros2 bag play bags/<bag-name>`.
